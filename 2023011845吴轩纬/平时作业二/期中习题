一、名词解释
地址映射：将程序中的逻辑地址转换为内存中的物理地址的过程。

动态重定位：在程序运行时进行的地址转换，通常由硬件重定位寄存器实现。

虚拟存储器：通过将部分程序和数据保存在外存中，为用户提供一个比实际内存大得多的存储空间。

静态链接：在程序运行前将所有目标模块和库函数链接成一个完整的可执行程序。

对换：将内存中暂时不用的进程或数据移到外存，腾出空间供其他进程使用。

设备驱动程序：操作系统与硬件设备之间的接口程序，负责控制设备的操作。

SPOOLing：Simultaneous Peripheral Operations Online，通过缓冲技术实现外围设备与CPU并行工作。

I/O通道：一种特殊的处理机，专门负责I/O操作，减轻CPU负担。

文件系统：操作系统中负责管理和存取文件信息的软件机构。

目标文件：编译器生成的包含机器代码但尚未链接的中间文件。

文件的逻辑结构：用户视角下文件的组织形式，如流式或记录式。

有结构文件：由若干相关记录组成的文件，如数据库文件。

位示图：用二进制位表示存储空间使用情况的磁盘管理数据结构。

程序接口：应用程序与操作系统交互的接口，如API。

系统调用：用户程序请求操作系统服务的接口。

I/O中断：I/O设备完成操作后向CPU发出的信号。

文件管理系统：操作系统中负责文件创建、删除、读写等操作的部分。

文件：具有名称的相关信息的集合，存储在外部存储器上。

文件的逻辑结构（重复）：同上11。

文件的物理结构：文件在存储设备上的实际存储方式。

二、填空题
动态重定位

绝对装入方式、可重定位装入方式、动态运行时装入方式

静态链接、装入时动态链接、运行时动态链接

空闲分区表、空闲分区链

紧凑（或拼接）

固定、系统、可变、程序

对换性、虚拟性

文件管理接口、文件管理软件、文件存储设备

分区分配、分区回收、分区保护

外部碎片

分页、分段、段页式

快表（TLB）

逻辑地址、物理地址

方便编程、信息共享、信息保护、动态增长、动态链接

地址变换机构

局部性、时间局部性、空间局部性

处理机、指令类型单一、没有自己的内存

顺序文件、索引文件、索引顺序文件

单级索引、多级索引、混合索引

动态重定位

中断处理程序、设备驱动程序、设备独立性软件

顺序结构、链接结构、索引结构、索引结构

高速缓存、主存、外存

连续分配、非连续分配

存储设备特性、存取方法

无结构文件（流式文件）、有结构文件（记录式文件）、索引文件

连续分配、链接分配、索引分配

绝对路径、相对路径

空闲盘块链、空闲盘区链

基于索引节点的共享、利用符号链实现共享

块设备、字符设备、独占设备、共享设备、虚拟设备

DMA方式、通道方式

单缓冲、循环缓冲、缓冲池

寻道时间、传输时间

三、简答题
计算机存储器系统层次：

寄存器：CPU内部，速度最快，容量最小

高速缓存：SRAM，速度快但容量有限

主存：DRAM，速度适中，容量较大

外存：磁盘/SSD，速度慢但容量大

分析：层次越高，速度越快，成本越高，容量越小

程序运行主要过程：

编辑：编写源代码

编译：将源代码转换为目标代码

链接：将目标代码与库函数链接成可执行程序

装入：将程序加载到内存

运行：CPU执行程序指令

终止：程序执行完毕释放资源

程序链接方式：

静态链接：运行前完成所有链接，生成完整可执行文件

装入时动态链接：装入内存时进行链接

运行时动态链接：程序运行时根据需要动态加载模块

异同：链接时机不同，静态链接效率高但灵活性差，动态链接节省内存但开销大

内存动态分区分配算法：

首次适应(FF)：从低地址开始查找第一个足够大的分区

循环首次适应(NF)：从上一次分配位置开始查找

最佳适应(BF)：查找大小最接近需求的分区

最坏适应(WF)：总是分配最大的可用分区

快速适应(QF)：按分区大小维护多个空闲链表

内存分区算法与CPU调度算法比较：

内存分区算法：FF、NF、BF、WF

CPU调度算法：FCFS、SJF、优先级、RR

相同点：都是资源分配策略

不同点：内存分配关注空间管理，CPU调度关注时间分配

页面置换算法与CPU调度算法比较：

页面置换：FIFO、LRU、OPT、CLOCK

CPU调度：同上

相同点：都涉及替换/切换策略

不同点：页面置换针对虚拟内存，CPU调度针对处理器时间

页面置换算法与磁盘调度算法比较：

磁盘调度：FCFS、SSTF、SCAN、C-SCAN

页面置换：同上

相同点：都涉及访问顺序优化

不同点：页面置换减少缺页，磁盘调度减少寻道时间

地址变换过程：

分页：逻辑地址→页号+页内偏移→查页表→物理地址

分段：逻辑地址→段号+段内偏移→查段表→物理地址

段页式：先分段再分页，需查段表和页表

中断处理步骤：

保护现场

识别中断源

执行中断服务程序

恢复现场

中断返回

文件类型分类：

用途：系统文件、用户文件、库文件

数据类型：源文件、目标文件、可执行文件

组织方式：普通文件、目录文件、特殊文件

文件目录分类：

单级目录：简单但易重名

二级目录：主目录+用户目录

树形目录：多级层次结构

查询方式：线性搜索、哈希、B树等

文件组织方式：

顺序文件：存取快但增删困难

索引文件：随机访问好但有额外开销

散列文件：查找快但空间利用率低

内存与外存分配方式：

内存：连续/非连续分配

外存：连续/链接/索引分配

相同点：都需管理空闲空间

不同点：内存注重速度，外存注重容量

提高磁盘I/O速度途径：

优化磁盘调度算法

使用磁盘高速缓存

采用RAID技术

提前读和延迟写

优化文件分配方式

请简述从计算机源程序到进程运行所经历的主要步骤、状态及相应存储位置。
从计算机源程序到进程运行的主要步骤、状态及存储位置
主要步骤及存储位置
编辑阶段

活动：程序员编写源代码

存储位置：外存（磁盘）中的源文件（如.c/.cpp/.java等）

状态：静态文本文件

编译阶段

活动：编译器将源代码翻译为目标代码

存储位置：外存中的目标文件（如.obj/.o）

状态：可重定位的机器代码

链接阶段

活动：链接器将目标文件与库函数合并

存储位置：外存中的可执行文件（如.exe/.out）

状态：完整的可执行程序映像

装入阶段

活动：操作系统将程序加载到内存

存储位置：从外存→主存（内存）

状态：内存中的程序映像

进程创建

活动：操作系统为程序创建进程控制块(PCB)

存储位置：主存中的内核区域（PCB）

状态：新建状态(New)

就绪状态

活动：进程等待CPU分配

存储位置：主存中的就绪队列

状态：就绪(Ready)

运行状态

活动：CPU执行进程指令

存储位置：主存中的代码段+数据段，CPU寄存器

状态：运行(Running)

阻塞状态（如需要）

活动：等待I/O等事件完成

存储位置：主存中的阻塞队列

状态：阻塞/等待(Blocked/Waiting)

终止状态

活动：进程执行完毕

存储位置：资源已释放

状态：终止(Terminated)

状态转换图
text
[编辑] → [编译] → [链接] → [装入] → [新建] → [就绪] ↔ [运行] ↔ [阻塞] → [终止]
 外存      外存     外存     外存→内存  内存     内存     内存     内存     释放
关键点说明
存储位置变化：

外存→内存：程序从磁盘加载到内存的过程

内存→CPU寄存器：进程运行时指令和数据被送入CPU

状态转换触发条件：

就绪→运行：被进程调度器选中

运行→就绪：时间片用完

运行→阻塞：等待I/O等资源

阻塞→就绪：等待的事件完成

虚拟内存系统：

在支持虚拟内存的系统中，部分进程可能暂时被换出到外存（交换区）

此时进程状态标记为"挂起"(Suspended)，是阻塞状态的变种

进程映像组成：

代码段：程序的执行指令

数据段：全局变量和静态变量

堆：动态分配的内存

栈：函数调用和局部变量

PCB：进程控制信息

这一过程展示了程序从静态的源代码到动态执行的进程的完整生命周期，涉及了计算机系统的主要存储层次和进程状态转换。

批操作系统，分时系统跟实时系统的判断优劣量
批处理系统、分时系统和实时系统的优劣比较
1. 批处理系统 (Batch Processing System)
优点：

高吞吐量：可以连续处理大量作业，减少系统空闲时间

资源利用率高：适合处理计算密集型任务，能充分利用CPU

自动化程度高：作业自动排队执行，无需人工干预

适合大型计算：对不需要交互的长时间运算非常有效

缺点：

无交互性：用户提交作业后无法与程序交互

响应时间长：用户必须等待一批作业全部完成

灵活性差：无法根据情况动态调整作业优先级

调试困难：程序错误只能在运行后被发现

适用场景：

科学计算

大规模数据处理

报表生成等非交互式任务

2. 分时系统 (Time-Sharing System)
优点：

良好的交互性：多个用户可同时与系统交互

快速响应：通过时间片轮转保证每个用户都能及时得到响应

资源共享：多个用户共享系统资源，提高资源利用率

公平性：每个用户/任务都能获得大致相等的CPU时间

缺点：

系统开销大：频繁的上下文切换消耗CPU资源

吞吐量较低：相比批处理系统，单位时间内完成的作业数较少

实时性不足：不适合对响应时间有严格要求的应用

复杂性高：需要复杂的调度和内存管理机制

适用场景：

多用户终端系统

一般用途的计算机系统

需要人机交互的应用

3. 实时系统 (Real-Time System)
优点：

确定性响应：能保证在严格时限内完成关键任务

高可靠性：通常有容错机制保证系统稳定运行

任务优先级明确：关键任务总能优先获得资源

可预测性：系统行为和时间特性可以精确预测

缺点：

资源利用率低：为保证实时性常保留大量冗余资源

开发成本高：需要专门的实时操作系统和开发工具

通用性差：通常专为特定应用设计

复杂性高：需要处理优先级反转、死锁等特殊问题

适用场景：

工业控制系统

航空航天系统

医疗设备

军事系统

金融交易系统

比较表格
比较维度	批处理系统	分时系统	实时系统
主要目标	高吞吐量	公平性和交互性	及时性和确定性
响应时间	长（小时/天）	短（秒级）	极短（毫秒/微秒级）
交互性	无	强	通常有限
用户数量	单用户	多用户	通常单应用
调度策略	先来先服务/短作业优先	时间片轮转	优先级调度
资源利用率	很高	中等	较低
系统开销	低	较高	高
典型应用	科学计算、报表生成	多用户终端、通用计算	工业控制、航空航天、医疗设备
选择建议
选择批处理系统当：

处理大量不需要交互的计算任务

追求最大吞吐量和资源利用率

可以接受较长的响应时间

选择分时系统当：

需要支持多个交互用户

需要公平分配计算资源

响应时间要求在秒级

选择实时系统当：

对响应时间有严格时限要求

系统失效可能导致严重后果

需要确定性的行为

现代操作系统常常结合多种系统的特点，如通用操作系统可能同时支持批处理作业和分时交互，而实时操作系统也可能提供有限的分时功能。选择时应根据具体应用需求权衡各种因素。

分别用什么量判断优劣
判断批处理系统、分时系统和实时系统的优劣，需要针对它们的设计目标和应用场景，采用不同的量化指标。以下是具体的评判标准：

1. 批处理系统的优劣判断指标
核心目标：最大化系统吞吐量（Throughput）和资源利用率
主要量化指标：

吞吐量（Throughput）

单位时间内完成的作业数量（如：作业/小时）

越高越好，反映系统处理批量任务的效率

CPU利用率（CPU Utilization）

CPU有效工作时间占总时间的比例（如：90%）

越高越好，减少CPU空闲时间

周转时间（Turnaround Time）

作业从提交到完成的总时间（如：小时/天）

越低越好，但批处理系统通常允许较长的周转时间

平均等待时间（Average Waiting Time）

作业在就绪队列中的平均等待时间

越低越好，反映调度算法的公平性

2. 分时系统的优劣判断指标
核心目标：提供公平、快速的交互响应
主要量化指标：

响应时间（Response Time）

用户输入命令到系统给出反馈的时间（如：毫秒/秒）

越低越好，通常要求在秒级以内

用户公平性（Fairness）

各用户获得的CPU时间比例是否均衡（如：标准差衡量）

越均衡越好，避免某些用户长期占用资源

系统开销（Overhead）

上下文切换、进程调度等额外消耗的时间占比

越低越好，否则会降低有效计算时间

并发用户数（Number of Concurrent Users）

系统能同时支持的交互用户数量（如：100个用户）

越高越好，但受限于CPU和内存资源

3. 实时系统的优劣判断指标
核心目标：严格保证任务的截止时间（Deadline）
主要量化指标：

截止时间满足率（Deadline Hit Rate）

关键任务在时限内完成的比率（如：99.99%）

越高越好，硬实时系统要求100%

最坏响应时间（Worst-Case Response Time, WCRT）

任务从触发到完成的最长时间（如：10ms）

必须小于截止时间，否则系统失效

抖动（Jitter）

任务执行时间的波动范围（如：±1ms）

越小越好，保证确定性

容错能力（Fault Tolerance）

系统在部分故障时仍能维持关键功能（如：冗余备份）

越强越好，适用于航空航天、医疗等关键领域

三类系统的对比表格
指标	批处理系统	分时系统	实时系统
核心目标	高吞吐量	快速交互	严格时限
关键指标	吞吐量、CPU利用率	响应时间、公平性	截止时间满足率、WCRT
典型数值	1000作业/小时	响应时间<1秒	WCRT<10ms
适用场景	科学计算、大数据	多用户终端、服务器	工业控制、自动驾驶
如何选择？
需要处理大量计算任务？ → 批处理系统（看吞吐量）

需要支持多用户交互？ → 分时系统（看响应时间和公平性）

有严格时间约束？ → 实时系统（看截止时间满足率和WCRT）

现代操作系统（如Linux、Windows）通常结合了分时和批处理特性，而专用实时系统（如VxWorks、QNX）则专注于确定性响应。选择时需根据具体需求权衡这些量化指标。

2025春期中习题课.pdf
PDF 239.9KB
2025春期中习题课解析
第一章 操作系统引论
一、名词解释
操作系统：管理和控制计算机硬件与软件资源的系统软件，是用户与计算机硬件之间的接口。

预输入：在批处理系统中，提前将作业输入到输入井中，减少CPU等待时间的技术。

分布式操作系统：管理分布式计算机系统的操作系统，特点是资源共享、透明性和容错性。

实时操作系统：能够在严格时间限制内完成特定任务的操作系统，分为硬实时和软实时。

互斥共享：资源一次只能被一个进程访问，其他进程必须等待的共享方式。

二、选择题
A：(2)提高系统资源的利用率；B：(3)资源

A：(2)系统软件；B：(2)与硬件相关而与应用无关；C：(4)高级程序设计语言的编译

A：(1)灵活性和可适应性；B：(3)周转时间和系统吞吐量；C：(4)实时性和可靠性

正确论述：(1)对批处理作业，必须提供相应的作业控制信息

A：(2)民航管理系统；B：(5)火箭飞行控制系统

三、填空题
硬件系统、软件系统

裸机

硬件资源、软件资源

顺序性

共享性、异步性

进程控制、调度

四、判断题
×（属于应用软件）

√

×（时间片过小会增加上下文切换开销）

√

√

×（硬实时系统才有此要求）

√

√

×（虚拟存储器是逻辑上扩充）

√

第二章 进程的描述与控制
一、名词解释
进程上下文：进程执行时CPU寄存器和内存状态的集合。

直接制约：进程间因共享资源而产生的相互等待关系。

临界区：访问共享资源的代码段，必须互斥执行。

进程同步：协调多个进程执行顺序的机制。

内核线程：由操作系统内核直接支持的线程。

二、选择题
A：(3)就绪；B：(4)执行；C：(2)阻塞

A：(2)6；B：(1)7

A：(2)用户登录；B：(2)作业调度；C：(4)提供服务；D：(3)为进程分配CPU

A：(1)1；B：(3)0；C：(5)n

(3)信号量的初值不能为负数

三、填空题
动态性、异步性

用户态、目态

进程控制信息、资源信息

内核空间

动态、静态

间接制约

空闲让进、忙则等待、有限等待、让权等待

共享内存、消息传递、管道通信

就绪、运行、阻塞

消息队列

四、判断题
×（不同进程可执行相同代码）

×（用户进程不能直接读取PCB）

√

×（变为就绪状态）

√

×（不能解决所有互斥问题）

×（顺序执行具有可再现性）

×（临界区是实现互斥的代码）

√

×（原语执行不可中断）

五、问答题
忙等：进程在等待资源时持续占用CPU检查条件，浪费CPU资源；饥饿：进程长期得不到所需资源，导致无法执行。忙等降低系统效率，饥饿影响公平性。

第三章 处理机调度与死锁
一、名词解释
作业：用户提交给计算机系统的一个计算任务单位。

处理机调度：从就绪队列中选择进程分配CPU的过程。

周转时间：作业从提交到完成的总时间。

死锁：多个进程因竞争资源而相互等待的僵局。

临时性资源：使用后即消失的资源，如信号量。

二、选择题
A：(3)后备；B：(2)周转时间；C：(4)时间片轮转

A：(2)短作业优先；B：(5)基于优先权的剥夺调度；C：(3)时间片轮转；D：(6)高响应比优先；E：(4)多级反馈队列；F：(2)短作业优先

A：(5)若干进程等待被其他进程所占用而又不可能被释放的资源；B：(2)系统资源不足；C：(1)进程推进顺序不当；D：(3)请求和保持条件；E：(2)环路条件

A：(2)资源有序分配法；B：(1)银行家算法

A：(2)一次性分配策略；B：(3)资源有序分配策略

三、填空题
联机输入、脱机输入、直接耦合、SPOOLing

提交、后备、完成

高级调度、低级调度、内存调度、对换

静态优先权、动态优先权

硬实时系统、软实时系统

预防、避免、检测、解除

四、判断题
×（不安全状态不一定死锁）

√

×（I/O型作业优先权通常更高）

×

√

√

×（还依赖于时限）

×（优先权高的队列时间片更长）

×（响应比=(等待时间+服务时间)/服务时间）

√

五、问答题
各调度算法结果：

FCFS：A(3),B(9),C(13),D(18),E(20)

非抢占SJF：A(3),B(9),E(11),C(15),D(20)

抢占SJF：A(3),B(9),E(11),C(15),D(20)

HRRN：A(3),B(9),C(13),D(18),E(20)

RR：A(4),B(18),C(17),D(20),E(12)

最低松弛度优先调度：

任务A周期20ms，执行10ms

任务B周期50ms，执行10ms

任务C周期50ms，执行15ms

调度顺序需保证各任务在截止时间前完成

银行家算法：
(1) 计算Allocation=Max-Need，检查安全序列（如P0→P3→P4→P1→P2）
(2) 检查Request≤Need和Request≤Available，假设可以分配
(3) 分配后检查是否安全，不会立即死锁

